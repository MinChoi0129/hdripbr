# hdripbr — 단일 conda env (Blackwell sm_120 / CUDA 12.8)
# torch는 cu128 인덱스에서 먼저 설치:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch==2.7.1 torchvision==0.22.1
# diffusers 0.32.2 고정 (DiT360 LoRA + OmniX 호환).

# -- core diffusion stack
diffusers==0.32.2
transformers==4.49.0          # Depth Anything V2 포함
accelerate==1.4.0
peft==0.19.1
sentencepiece==0.2.0
safetensors==0.8.0rc0
tokenizers==0.21.4
huggingface_hub
omegaconf==2.3.0

# -- numerics / imaging
numpy==2.4.4
scipy==1.17.1
einops==0.8.1
matplotlib==3.10.9
pillow==12.2.0
opencv-python==4.13.0.92
scikit-image
imageio==2.37.0
tqdm==4.67.3
PyYAML==6.0.3

# -- DeepBump (AO/Height/Displacement) ONNX 추론
onnxruntime==1.27.0
